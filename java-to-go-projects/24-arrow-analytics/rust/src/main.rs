mod loader;
mod compute;

fn main() {
    println!("Arrow Columnar Analytics Demo");
    todo!("1) Load CSV into RecordBatch, 2) compute sum/avg/min/max, 3) filter rows, 4) group-by aggregation, 5) write Arrow IPC file, 6) read back and verify")
}
