mod engine;
mod iterator;

fn main() {
    println!("RocksDB Storage Engine Demo");
    todo!("1) Open engine, 2) create 'events' and 'index' column families, 3) batch write 10k events, 4) scan key range, 5) snapshot + concurrent write test, 6) compact, print stats, 7) close")
}
